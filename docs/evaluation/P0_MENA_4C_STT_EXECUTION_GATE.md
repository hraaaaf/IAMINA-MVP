# P0-MENA-4C — STT benchmark execution gate

Live speech-to-text evaluation remains blocked until every candidate has current evidence for training use, general retention, audio-specific retention, residency and subprocessors, plus an approved synthetic-only credential.

## Prepared

- fail-closed STT provider manifest;
- audio-retention-specific approval field;
- credential lookup outside source control;
- canonical STT-only dataset selection;
- provider adapter boundary;
- tests proving no adapter is created before authorization.

## Still external

- approved synthetic audio fixtures;
- exact provider/model/region selection;
- current contractual evidence;
- authorized credentials and cost budget;
- native review of Darija, MSA, French, English and mixed-language transcripts.

No score or provider decision is inferred from readiness of this infrastructure.
