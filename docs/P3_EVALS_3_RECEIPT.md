# P3-EVALS-3 Receipt Semantics

`Receipt` is a storage and validation format only.

It records an actor, timezone-aware timestamp, covered item identifiers, and a status value.

A stored receipt does not by itself prove that a human review occurred, does not create clinical authority, and must not be interpreted as an approval unless a real external review supplied that evidence.
