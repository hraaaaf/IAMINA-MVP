name: Native Voice batched benchmark

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/native-voice-batched-benchmark.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: native-voice-batched-benchmark
  cancel-in-progress: false

jobs:
  groq-native-voice-batched:
    name: Groq synthetic Native Voice 7-call batched benchmark
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      DJANGO_SETTINGS_MODULE: amina.settings
      SECRET_KEY: native-voice-batched-benchmark-nonproduction
      DEBUG: "True"
      ALLOWED_HOSTS: localhost,127.0.0.1
      DATABASE_URL: sqlite:///native-voice-batched-benchmark.db
      GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
      NATIVE_VOICE_NETWORK_AUTHORIZED: "true"

    steps:
      - name: Checkout exact main
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install backend dependencies
        working-directory: backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Validate batched benchmark contracts before network
        working-directory: backend
        run: |
          pytest -q             evaluation/test_native_voice_shadow_benchmark.py             evaluation/test_native_voice_live_benchmark.py             evaluation/test_native_voice_batched_benchmark.py

      - name: Run bounded batched Native Voice benchmark
        working-directory: backend
        run: |
          mkdir -p ../artifacts
          python -m evaluation.native_voice_batched_benchmark             --output ../artifacts/native-voice-batched-benchmark.json

      - name: Upload Native Voice batched review packet
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: native-voice-batched-benchmark
          path: artifacts/native-voice-batched-benchmark.json
          if-no-files-found: warn
          retention-days: 14
