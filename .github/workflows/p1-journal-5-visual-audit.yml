name: P1-JOURNAL-5 visual audit

on:
  push:
    branches: [audit/p1-journal-5-visual]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  visual-audit:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout audit harness
        uses: actions/checkout@v4
        with:
          path: audit
      - name: Checkout exact product SHA
        uses: actions/checkout@v4
        with:
          ref: 9ba9122973735174f23f22cde70baefb748c6266
          path: product
      - name: Overlay audit-only harness
        run: |
          mkdir -p product/frontend/test/audit
          cp audit/frontend/test/audit/p1_journal_5_visual_audit_test.dart product/frontend/test/audit/
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.0'
          channel: stable
          cache: true
      - name: Resolve dependencies
        working-directory: product/frontend
        run: flutter pub get
      - name: Render FR/AR add+edit matrix
        working-directory: product/frontend
        run: flutter test --update-goldens test/audit/p1_journal_5_visual_audit_test.dart
      - name: Upload rendered matrix
        uses: actions/upload-artifact@v4
        with:
          name: p1-journal-5-visual-matrix-9ba9122
          path: product/frontend/test/audit/goldens/*.png
          if-no-files-found: error
