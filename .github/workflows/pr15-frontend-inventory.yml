name: PR15 frontend inventory

on:
  push:
    branches:
      - feat/p0-mena-provider-runtime-resilience

jobs:
  inventory:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: pr15-frontend-inventory
          path: |
            frontend/lib
            frontend/test
