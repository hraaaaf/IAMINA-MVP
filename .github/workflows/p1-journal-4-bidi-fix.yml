name: P1-JOURNAL-4 bidi repair

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'One-shot repair completed; retained for audit provenance only.'
