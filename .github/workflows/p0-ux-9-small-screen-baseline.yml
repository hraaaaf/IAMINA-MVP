name: P0-UX-9 small-screen baseline

on:
  push:
    branches:
      - audit/p0-ux-9-small-screen-baseline

permissions:
  contents: read

jobs:
  capture:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: 21f7696a659f45c379e59e5986aca64f175ee16b

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.0'
          channel: stable
          cache: true

      - name: Build certified Flutter Web bundle
        working-directory: frontend
        run: |
          flutter pub get
          flutter build web --release --dart-define=IAMINA_AUDIT_ACCESS=true

      - name: Install capture runtime
        run: |
          npm init -y >/dev/null 2>&1
          npm install playwright@1.54.2
          npx playwright install --with-deps chromium

      - name: Start local certified bundle
        working-directory: frontend/build/web
        run: |
          python3 -m http.server 8080 --bind 127.0.0.1 > /tmp/iamina-web.log 2>&1 &
          for i in {1..30}; do
            curl -fsS http://127.0.0.1:8080/ >/dev/null && exit 0
            sleep 1
          done
          cat /tmp/iamina-web.log
          exit 1

      - name: Capture 360x560 FR/AR matrix
        run: |
          mkdir -p /tmp/p0-ux-9
          cat > /tmp/p0-ux-9/capture.cjs <<'EOF'
          const { chromium } = require(process.cwd() + '/node_modules/playwright');
          const fs = require('fs');
          const path = require('path');

          const routes = [
            ['dashboard', '/dashboard'],
            ['summary', '/summary'],
            ['journal', '/journal'],
            ['importer', '/importer'],
            ['profile', '/profile'],
          ];
          const profiles = [
            ['small-fr', 360, 560, 'fr', 'fr-FR'],
            ['small-ar', 360, 560, 'ar', 'ar-MA'],
          ];

          (async () => {
            const browser = await chromium.launch({ headless: true });
            const report = [];
            for (const [profile, width, height, lang, browserLocale] of profiles) {
              const context = await browser.newContext({
                viewport: { width, height },
                deviceScaleFactor: 1,
                locale: browserLocale,
                timezoneId: 'Africa/Casablanca',
              });
              for (const [name, route] of routes) {
                const page = await context.newPage();
                const consoleErrors = [];
                const pageErrors = [];
                page.on('console', msg => { if (msg.type() === 'error') consoleErrors.push(msg.text()); });
                page.on('pageerror', err => pageErrors.push(String(err)));
                const url = `http://127.0.0.1:8080/?audit=visual-cert&lang=${lang}#${route}`;
                await page.goto(url, { waitUntil: 'domcontentloaded', timeout: 30000 });
                await page.waitForTimeout(6000);
                const file = `${profile}__${name}.png`;
                await page.screenshot({ path: path.join('/tmp/p0-ux-9', file), fullPage: true });
                const flutterViews = await page.locator('flutter-view').count();
                report.push({ profile, name, route, lang, width, height, url: page.url(), flutterViews, consoleErrors, pageErrors, screenshot: file });
                await page.close();
              }
              await context.close();
            }
            await browser.close();
            fs.writeFileSync('/tmp/p0-ux-9/report.json', JSON.stringify(report, null, 2));
            const failures = report.filter(x => x.flutterViews === 0 || x.pageErrors.length > 0);
            if (failures.length) {
              console.error(JSON.stringify(failures, null, 2));
              process.exit(1);
            }
          })();
          EOF
          node /tmp/p0-ux-9/capture.cjs

      - name: Record certified source
        if: always()
        run: |
          git rev-parse HEAD > /tmp/p0-ux-9/source-sha.txt
          cp /tmp/iamina-web.log /tmp/p0-ux-9/server.log
          find /tmp/p0-ux-9 -maxdepth 1 -type f -printf '%f %s bytes\n' | sort > /tmp/p0-ux-9/manifest.txt

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: p0-ux-9-small-screen-baseline
          path: /tmp/p0-ux-9
          retention-days: 14
          if-no-files-found: error
