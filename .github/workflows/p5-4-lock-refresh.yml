name: P5-4 temporary lock refresh

on:
  pull_request:
    branches: [main]
    paths:
      - "frontend/pubspec.yaml"
      - ".github/workflows/p5-4-lock-refresh.yml"

permissions:
  contents: write

concurrency:
  group: p5-4-lock-refresh-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  refresh:
    if: >-
      github.event.pull_request.number == 521 &&
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.41.0"
          channel: stable
          cache: true

      - name: Verify current voice callsite contract before dependency refresh
        run: |
          set -euo pipefail
          FILE="frontend/lib/features/journal/widgets/amina_chat_view.dart"
          git grep -n "package:record/record.dart" -- "$FILE"
          git grep -n "AudioRecorder" -- "$FILE"
          git grep -n "RecordConfig" -- "$FILE"
          git grep -n "startStream" -- "$FILE"
          git grep -n "_recorder.stop" -- "$FILE"

      - name: Regenerate lockfile with pinned Flutter toolchain
        working-directory: frontend
        run: flutter pub get

      - name: Commit only regenerated lockfile
        run: |
          set -euo pipefail
          if git diff --quiet -- frontend/pubspec.lock; then
            echo "Lockfile already coherent"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add frontend/pubspec.lock
          test "$(git diff --cached --name-only)" = "frontend/pubspec.lock"
          git commit -m "build(frontend): refresh lockfile for record 6.2.1"
          git push origin "HEAD:${{ github.head_ref }}"
