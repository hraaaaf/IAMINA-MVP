name: UX-4 exact-head visual

on:
  push:
    branches:
      - audit/ux-4-final-0b3cdd0
    paths:
      - .github/workflows/ux-4-final-visual.yml

permissions:
  contents: read

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
        with:
          ref: 0b3cdd0ede243f5ad60e7bdfc766316230cc776b
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.0'
          channel: stable
          cache: true
      - name: Build
        working-directory: frontend
        run: |
          flutter pub get
          flutter build web --release --dart-define=IAMINA_AUDIT_ACCESS=true
      - name: Install capture runtime
        run: |
          npm init -y >/dev/null 2>&1
          npm install playwright@1.54.2
          npx playwright install --with-deps chromium
      - name: Serve
        working-directory: frontend/build/web
        run: |
          python3 -m http.server 8080 --bind 127.0.0.1 >/tmp/iamina-web.log 2>&1 &
          for i in {1..30}; do curl -fsS http://127.0.0.1:8080/ >/dev/null && exit 0; sleep 1; done
          exit 1
      - name: Capture Summary degraded matrix
        run: |
          mkdir -p /tmp/ux4final
          cat >/tmp/ux4final/capture.cjs <<'EOF'
          const { chromium } = require(process.cwd() + '/node_modules/playwright');
          const fs = require('fs'); const path = require('path');
          const langs=[['fr','fr-FR'],['ar','ar-MA']];
          const viewports=[['desktop',1440,1000],['tablet',768,1024],['mobile',390,844],['small',360,560]];
          const cases=[];
          for(const [vp,w,h] of viewports) for(const [lang,locale] of langs) cases.push([vp,w,h,lang,locale]);
          (async()=>{const b=await chromium.launch({headless:true}); const report=[];
            for(const [vp,w,h,lang,locale] of cases){
              const c=await b.newContext({viewport:{width:w,height:h},locale,timezoneId:'Africa/Casablanca'}); const p=await c.newPage();
              const pageErrors=[]; p.on('pageerror',e=>pageErrors.push(String(e)));
              await p.goto(`http://127.0.0.1:8080/?audit=visual-cert&lang=${lang}#/summary`,{waitUntil:'domcontentloaded',timeout:30000});
              await p.waitForTimeout(5000);
              const file=`${vp}-${lang}__summary.png`; await p.screenshot({path:path.join('/tmp/ux4final',file),fullPage:true});
              report.push({vp,lang,w,h,url:p.url(),flutterViews:await p.locator('flutter-view').count(),pageErrors,screenshot:file}); await c.close();
            }
            await b.close(); fs.writeFileSync('/tmp/ux4final/report.json',JSON.stringify(report,null,2));
            if(report.some(x=>x.flutterViews!==1||x.pageErrors.length)) process.exit(1);
          })();
          EOF
          node /tmp/ux4final/capture.cjs
      - name: Record source
        if: always()
        run: echo 0b3cdd0ede243f5ad60e7bdfc766316230cc776b >/tmp/ux4final/source-sha.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: ux-4-final-visual
          path: /tmp/ux4final
          retention-days: 14
