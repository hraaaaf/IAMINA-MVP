name: P0 Auth Migrations

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  makemigrations:
    if: github.head_ref == 'fix/p0-auth-profile-integrity'
    runs-on: ubuntu-latest
    env:
      DJANGO_SETTINGS_MODULE: amina.settings
      SECRET_KEY: ci-secret-key-not-for-production
      DEBUG: 'True'
      ALLOWED_HOSTS: localhost,127.0.0.1
      DATABASE_URL: sqlite:///ci-test.db
      FIREBASE_CREDENTIALS_PATH: ''
      GEMINI_API_KEY: ''
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: backend/requirements.txt
      - run: pip install -r backend/requirements.txt
      - name: Generate deterministic model migrations
        run: |
          cd backend
          python manage.py makemigrations core diabetes --noinput
      - name: Commit migrations
        run: |
          if git diff --quiet -- backend/core/migrations backend/diabetes/migrations; then
            echo 'No migrations generated'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add backend/core/migrations backend/diabetes/migrations
          git commit -m 'db(p0): allow undeclared patient profile fields to remain null'
          git push origin HEAD:${{ github.head_ref }}
