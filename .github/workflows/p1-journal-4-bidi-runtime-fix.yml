name: P1-JOURNAL-4 runtime bidi fix

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  provenance:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'One-shot runtime bidi remediation completed; retained only for audit provenance.'
