name: P1-JOURNAL-4 formatter cleanup

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  provenance:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'One-shot formatter cleanup completed; retained only for audit provenance.'
